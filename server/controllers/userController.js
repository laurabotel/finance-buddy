const User = require('../../models.userModel.js');

const userController = {
  addUser: () => {},
  findUser: () => {},
  saveProjection: () => {},
  findProjection: () => {},
};

module.exports = userController;
